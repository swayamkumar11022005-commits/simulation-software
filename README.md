18th september 26

right now, 

we can solve resistance networks with voltage and current sources (linear)

and diodes (non linear)
we have implemented

DC sweep 
and 
transient analysis for capacitors

but the current probelm is the flexibility like we cant used DC sweep for now, because 
the parser has no ability to classify what operation should be done

and for GUI streamlit is shit, we can use ImGUI a C++ library for the UI but
now i think maam has really high standards 

but that doesnt mean we must fullfil her ever need, so for GUI we can add some buttons and
a netlist text editor popup and then after that some options what to perform.

and according to maam, she wants us to make a UI in which we can draw the circuit and it converts into
netlist and then we run operations
(its possible but it will take time)


for now we should add buttons to what operation to perform on the ckt.
thats all i thikn. if you got some creative idea go bonkers on GC

